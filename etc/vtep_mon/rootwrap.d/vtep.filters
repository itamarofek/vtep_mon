# command filters
# expected to control network
#
# This file should be owned by (and only-writeable by) the root user

# format seems to be
# cmd-name: filter-name, raw-command, user, args

[Filters]

# openvswitch-agent
# unclear whether both variants are necessary, but I'm transliterating
# from the old mechanism
ovs-vsctl: CommandFilter, ovs-vsctl, root
vtep-ctl: CommandFilter, vtep-ctl, root
ovs-vtep: CommandFilter, ovs-vtep, root
ovs-appctl: CommandFilter, ovs-appctl, root
ovsdb-client: CommandFilter, ovsdb-client, root
ovsdb-tool: CommandFilter, ovsdb-tool, root
# NOTE(yamamoto): of_interface=native doesn't use ovs-ofctl
ovs-ofctl: CommandFilter, ovs-ofctl, root
kill_ovsdb_client: KillFilter, root, /usr/bin/ovsdb-client, -9
kill_ovsdb_server: KillFilter, root, /usr/bin/ovsdb-server, -HUP
xe: CommandFilter, xe, root

# ip_lib
ip: IpFilter, ip, root
find: RegExpFilter, find, root, find, /sys/class/net, -maxdepth, 1, -type, l, -printf, %.*
ip_exec: IpNetnsExecFilter, ip, root
ifup: CommandFilter, ifup, root
ifdown: CommandFilter, ifdown, root
ifconfig: CommandFilter, ifconfig, root
#service
service: CommandFilter, service, root
rm:CommandFilter,rm,root
cp:CommandFilter,cp,root
